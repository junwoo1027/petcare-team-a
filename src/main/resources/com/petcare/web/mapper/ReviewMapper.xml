<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petcare.web.mapper.ReviewMapper">
<insert id="insert">
	insert into review(
		userid, content, rating, hospitalid
	) values(
		#{userid}, #{content}, #{rating}, #{hospitalid}
	)
</insert>

<select id="getList" resultType="com.petcare.web.domain.ReviewVo">
	select reviewNo, userid, content, rating, regdate, hospitalid
	from review
	where hospitalid = #{hospitalid}
	order by reviewNo asc
	limit #{cri.pageStart}, #{cri.amount}
</select>

<select id="getCountByHospitalid" resultType="int">
    	select count(*) from review where hospitalid = #{hospitalid}
</select>

<delete id="delete">
		delete from review where reviewNo = #{reviewNo}
</delete>
</mapper>